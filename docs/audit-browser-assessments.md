
## Cleanup verification

After an authenticated API request initialized the local database, the deterministic cleanup removed empty seed duplicates and reparented their attempts to the canonical seeded exam. The browser page now shows exactly two meaningful cards for the audit account: one user-created two-question mixed assessment and one canonical three-question Python checkpoint. The seeded checkpoint now reports three questions instead of zero, and duplicate cards are gone.

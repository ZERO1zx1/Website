The code lab page rendered correctly with its challenge sidebar, editor, language selector, and output panel. On `/auth?mode=register`, the visual page loaded but stayed in the login state because the existing Content-Security-Policy blocked the new inline page scripts. This must be fixed before final verification so registration tabs and editor interactions work.
After changing the CSP and starting a fresh instance on port 5001, `/auth?mode=register` correctly activated the registration state with name and consent fields plus the `Бүртгэл үүсгэх` CTA. `/course?id=python` correctly rendered the Python hero, 12 lessons across four modules, all lesson links, and the lab handoff card.
